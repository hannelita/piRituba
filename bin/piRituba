#!/usr/bin/ruby
#encoding: utf-8
while true
  trap("INT") do 
    puts "\nVocê sai de piRituba mas piRituba não sai de você"
    while x = gets 
      if(x.strip == "NOIS")
        exit(0)
      end
      puts x
    end
  end
end